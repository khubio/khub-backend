const slideType = {
  multipleChoice: 'multipleChoice',
  heading: 'heading',
  paragraph: 'paragraph',
};

const accessModifier = {
  public: 'public',
  group: 'group',
  private: 'private',
};

const chartType = {
  bars: 'bars',
  donut: 'donut',
  pie: 'pie',
  dots: 'dots',
};

module.exports = {
  slideType,
  accessModifier,
  chartType,
};
